import 'package:flutter/material.dart';


class ShopListView extends StatelessWidget{
  const ShopListView({
    super.key,
  });

  @override 
  Widget build(BuildContext context) {
    return Center(
      child: Text("Aqui van las compras"),
    );
  }

}